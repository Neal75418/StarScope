/**
 * 自訂 DOM 事件名稱。
 * 用於跨元件樹的通訊（例如 Settings → NotificationStorage / WatchlistProvider）。
 */

export const DATA_RESET_EVENT = "starscope:data-reset";
export const RATE_LIMITED_EVENT = "starscope:rate-limited";

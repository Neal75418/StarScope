# FastAPI and server
fastapi>=0.115.8
pydantic>=2.0.0  # Explicitly listed for IDE support (bundled with FastAPI)
# Note: starlette is managed by FastAPI - do not pin separately
uvicorn[standard]>=0.34.0
python-dotenv>=1.0.0

# HTTP client for GitHub API
httpx>=0.28.0

# Retry with exponential backoff
tenacity>=9.0.0

# Data processing
pandas>=3.0.0

# Database
sqlalchemy>=2.0.46
alembic>=1.14.0

# Background scheduler
apscheduler>=3.10.0

# For future PyInstaller packaging
pyinstaller>=6.0.0

# Testing
pytest>=9.0.0  # CVE-2025-71176: wait for 9.1.0 release
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx[http2]>=0.28.0
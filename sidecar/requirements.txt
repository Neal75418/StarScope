# FastAPI and server
fastapi>=0.115.8
pydantic>=2.0.0  # Explicitly listed for IDE support (bundled with FastAPI)
# Keep starlette unpinned to align with FastAPI's dependency range.
starlette
uvicorn[standard]>=0.34.0
python-dotenv>=1.0.0
keyring>=25.0.0

# HTTP client for GitHub API
httpx>=0.28.0

# Retry with exponential backoff
tenacity>=9.0.0

# Data processing
pandas>=3.0.0,<4.0.0

# Database
sqlalchemy>=2.0.46
alembic>=1.14.0

# Background scheduler
apscheduler>=3.10.0

# API rate limiting
slowapi>=0.1.9

# For future PyInstaller packaging
pyinstaller>=6.0.0

# Testing
pytest>=9.0.0  # CVE-2025-71176: update to >=9.1.0 when released (latest: 9.0.2)
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx[http2]>=0.28.0
